{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "DNAmDeconv pipeline - params.bamsheet schema",
    "description": "Schema for the file provided with params.bamsheet",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "name": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "type": {
                "type": "string",
                "pattern": "^[a-zA-Z]+$",
                "errorMessage": "Sample type must be provided, cannot contain spaces or numbers but only characters"
            },
            "bam": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.bam$",
                "errorMessage": "Path for the bam file must be provided and cannot contain spaces"
            },
            "bai": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.bam.bai$",
                "errorMessage": "Path for the bai file must be provided and cannot contain spaces"
            }
        },
        "required": ["name", "type", "bam", "bai"]
    }
}

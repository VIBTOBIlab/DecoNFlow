{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "DNAmDeconv pipeline - params.test_set schema",
    "description": "Schema for the file provided with params.test_set",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "name": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "sample": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.cov\\.gz$",
                "errorMessage": "Path for the coverage file must be provided and cannot contain spaces"
            }
        },
        "required": ["name", "sample"]
    }
}

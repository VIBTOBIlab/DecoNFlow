nextflow_process {

    name "Test Process CONVERT_ATLAS"
    script "modules/convert_atlas/main.nf"
    process "CONVERT_ATLAS"

    test("convert atlas | mean collapse method") {

        when {
            params {
                // define parameters here. Example:
                collapse_method = 'mean'
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(
                            file(params.pipelines_testdata_base_path + 'modules/limma/atlas_matrix.csv')
                            )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

    test("convert atlas | median collapse method") {

        when {
            params {
                // define parameters here. Example:
                collapse_method = 'median'
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(
                            file(params.pipelines_testdata_base_path + 'modules/limma/atlas_matrix.csv')
                            )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }
}

nextflow_process {

    name "Test Process EPIDISH"
    script "modules/refbased_tools/epidish/main.nf"
    process "EPIDISH"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(
                                file(params.pipelines_testdata_base_path + '/modules/limma/reference_matrix_limma.csv')
                            )
                input[1] = Channel.of(
                                file(params.pipelines_testdata_base_path + '/modules/limma/test_matrix.csv')
                            )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}

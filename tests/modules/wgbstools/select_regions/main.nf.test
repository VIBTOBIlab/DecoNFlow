nextflow_process {

    name "Test Process SELECT_REGIONS"
    script "modules/wgbstools/select_regions/main.nf"
    process "SELECT_REGIONS"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(file(params.pipelines_testdata_base_path + '/modules/wgbstools/Atlas.l3.tsv'))
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}

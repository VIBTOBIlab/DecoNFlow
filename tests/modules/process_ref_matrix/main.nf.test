nextflow_process {

    name "Test Process PROCESS_REF_MATRIX"
    script "modules/process_ref_matrix/main.nf"
    process "PROCESS_REF_MATRIX"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(
                            file(params.pipelines_testdata_base_path + '/modules/limma/reference_matrix_limma.tsv')
                            )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}

nextflow_process {

    name "Test Process METDECODE_DECONV"
    script "modules/metdecode/main.nf"
    process "METDECODE_DECONV"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(file(params.pipelines_testdata_base_path + '/modules/metdecode/atlas.csv'))
                input[1] = Channel.of(file(params.pipelines_testdata_base_path + '/modules/metdecode/samples.csv'))
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}

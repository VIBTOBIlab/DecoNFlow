nextflow_process {

    name "Test Process BISMARK_METHYLATIONEXTRACTOR"
    script "modules/bismark_methylation_extractor/main.nf"
    process "BISMARK_METHYLATIONEXTRACTOR"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                single_end = true
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of([
                    "SRR1045638",
                    "lung_1",
                    file(params.pipelines_testdata_base_path + '/bams/SRR1045638_chr1_1M.bam'),
                    file(params.pipelines_testdata_base_path + '/bams/SRR1045638_chr1_1M.bam.bai')]
                    )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}

nextflow_process {

    name "Test Process LIMMA"
    script "modules/dmr_selection_limma/limma/main.nf"
    process "LIMMA"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(
                                file(params.pipelines_testdata_base_path + 'modules/limma/atlas_matrix.csv')
                                )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}

nextflow_process {

    name "Test Process MERGE_TABLES"
    script "modules/celfie/merge_tables/main.nf"
    process "MERGE_TABLES"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(file(params.pipelines_testdata_base_path + '/modules/celfie/final_celfie_atlas_matrix.csv'))
                input[1] = Channel.of(file(params.pipelines_testdata_base_path + '/modules/celfie/test_celfie_matrix.csv'))
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}

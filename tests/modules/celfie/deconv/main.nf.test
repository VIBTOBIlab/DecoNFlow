nextflow_process {

    name "Test Process RUN_DECONV"
    script "modules/celfie/deconv/main.nf"
    process "RUN_DECONV"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = file(params.pipelines_testdata_base_path + '/modules/celfie/celfie_matrix.txt')
                input[1] = 3
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
